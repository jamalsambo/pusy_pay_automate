'use strict';
const express = require('express');
const router = express.Router();
const controller = require('./controller');

// router.post('/transaction-status', controller.transactionStatus)

module.exports = router;